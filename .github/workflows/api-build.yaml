name: API CI Build

on:
  pull_request:
    branches: [develop]

jobs:
  build:
    name: Node API Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.4.0'

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: npm run build